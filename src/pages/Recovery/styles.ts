import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#FFF',
        padding: 20
    },
    logo: {
        width: 200,
        height: 200,
        marginBottom: 40
    },
});